<div class="second_nav">
    <div class="dropdown">
        <button class="btn-secondary dropdown-toggle btn-change" type="button" data-bs-toggle="dropdown" aria-expanded="false">
            Catégories
        </button>
        <ul class="dropdown-menu">
            {% for category in categories %}
                <li>
                    <div class="list-objects">
                        <img width="25px" src="{{ category.picture }}">
                        <a class="dropdown-item" href="{{ path('app_categories_page', {id:category.id}) }}">{{ category.name }}</a>
                    </div>
                </li>
            {% endfor %}
        </ul>
    </div>
</div>